#include <bits/stdc++.h>
using namespace std;

int nodeNum;
int x;
int count = 0;

int main() {
    cin >> nodeNum;
    vector <int> vertex(nodeNum);
    vector <pair<int, int>> paths(nodeNum);

    for (int i = 1; i <= nodeNum; i++) {
        cin >> x;
        vertex.push_back(x);
    }

    for (int i = 1; i <= nodeNum; i++) {}
}