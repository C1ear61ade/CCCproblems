#include <bits/stdc++.h>
using namespace std;
int numberofInputs;
int main() {
    cin >> numberofInputs;
    vector<string> numbers(numberofInputs);
    for (int i = 0; i < numberofInputs; ++i) {
        string input;
        cin >> numbers[i];
    }
    for (int j = 0; j < numbers.size(); ++j) {
        for (int k = 0; k < numbers[j].length(); ++k) {

        }
    }
}